import Lecture from '../models/lecture.models';

export const createLecture = async (req, res) => {
    
};

// dipakai untuk mencari lecture seharusnya yang didatangi user
export const getCurrentLecture = async (req, res) => {
    
};

// dipakai untuk menampilkan lecture Bulan ini (view student)
export const getMonthlyLecture = async (req, res) => {
    
};